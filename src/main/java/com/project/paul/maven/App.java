package com.project.paul.maven;

import java.util.Map;
import java.util.HashMap;

/**
 * This is the App.java file. To execute unit tests, type in terminal 'mvn
 * test'.
 *
 *
 * // -- File AmMachine contains the logic for calculating costs using Decorator
 * classes.
 *
 * This will execute the ChallengeOneTest.java to verify that the following are
 * true:
 * - A medium power machine with with a reduced build volume, a quad laser
 * system, and powder recirculation system costs a total of £932,000.
 *
 * // -- File AlloyOptimisation contains the logic for alloys, elements + interface (Containing min, max, step, atomic %, cost, creep coefficient)
 *
 * The ChallengeTwoTest.java will also be executed to verify that the following
 * are true:
 * - The best alloy found has a creep resistance of 1.72999E18 and a cost below
 * £18.
 *
 */

public class App {
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}
